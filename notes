Plans
 Make a inline variables for function
 Make a throw function that throws a error if false
 Test booleans and add Boolean operations
 If Else If Else
 Random value function
 Comments break when single quote > '
